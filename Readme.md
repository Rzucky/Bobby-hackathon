# Bobby tables KojiKukac hackathon

## Local development

Application is containerized, to run it use:

```sh
$ docker build
$ docker compose up -d
```

## Credentials

Credentials are located in `.env` file, just copy `.env.example` and use real data
